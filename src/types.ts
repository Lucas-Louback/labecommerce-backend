export type TUser = {
    id: string;
    name: string;
    email: string;
    password: string;
    createdAt: string;
}

export type TProducts = {
    id: string;
    name: string;
    price: number;
    description: string;
    imageUrl: string;
}

export type TProducts2 = {
    id: string;
    name: string;
    price: number;
    description: string;
    image_url: string;
}